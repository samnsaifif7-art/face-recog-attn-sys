/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you deploy to Vercel later, this is already fine.
  // Nothing special is needed for calling your Render API.
};

module.exports = nextConfig;
